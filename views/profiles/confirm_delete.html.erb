<div class="container">
  <div class="card shadow-lg rounded-lg">
    <div class="card-body text-center">
      <h2 class="mb-4 text-danger">Confirm Account Deletion</h2>

      <p><strong>Warning:</strong> Deleting your account is permanent and cannot be undone.</p>

      <p class="mt-3"><strong>This action will:</strong></p>

      <!-- Aligned bullet points -->
      <ul class="delete-warning-list">
        <li>Remove all your data from our system.</li>
        <li>Log you out of all devices.</li>
        <li>Require you to register again if you wish to use the service in the future.</li>
      </ul>

      <p class="mt-4"><strong>To proceed, please enter your GUID:</strong></p>

      <%= form_with url: profile_path, method: :delete, html: { class: "needs-validation" } do %>
        <%= hidden_field_tag :authenticity_token, form_authenticity_token %>

          <%= text_field_tag :guid, nil, required: true, placeholder: "Enter your GUID", class: "form-control text-center" %>
        </div>

        <div class="d-grid gap-3 mt-5"> <%# More spacing above buttons %>
          <%= link_to "Cancel", profile_path, class: "btn btn-secondary btn-lg" %>
          <%= submit_tag "Confirm & Delete Account", data: { confirm: "Are you sure you want to permanently delete your account?" }, class: "btn btn-danger btn-lg" %>
        </div>
      <% end %>

    </div>
  </div>
</div>
